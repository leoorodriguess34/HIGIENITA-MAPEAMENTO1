name: Sincronizar Tiny ERP com Firebase (desligado)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Integracao desligada
        run: echo "Integracao Tiny desligada. Este workflow nao chama a API do Tiny."
