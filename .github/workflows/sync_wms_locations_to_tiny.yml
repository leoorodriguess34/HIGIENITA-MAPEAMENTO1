name: Atualizar localizacoes WMS no Tiny (desligado)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Integracao desligada
        run: echo "Atualizacao WMS -> Tiny desligada. Este workflow nao chama a API do Tiny."
